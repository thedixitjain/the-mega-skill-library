# Boundary Alignment Examples

Read only one example file, matching the project language or files being edited. Skip examples when the core checkpoint is enough.

- TypeScript / JavaScript / Node / React: `examples-typescript.md`
- Python: `examples-python.md`

If the project language is not listed, use the closest typed example only for boundary-handling principles, then implement in the project's existing style.

These are compact patterns, not copy-paste templates. Preserve the current project's architecture, names, libraries, error types, and test style.

If no matching example file is listed, continue from the core protocol and `anti-patterns.md`; do not invent pseudocode or switch languages.
