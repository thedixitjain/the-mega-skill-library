interface:
  display_name: "Obsidian CLI Runtime Admin"
  short_description: "Manage plugins, themes, snippets, and command/hotkey runtime surfaces via official Obsidian CLI."
  default_prompt: "Use official Obsidian CLI runtime-admin commands to inspect or change plugins, themes, snippets, or command IDs with explicit side-effect reporting and conservative execution guardrails."
policy:
  allow_implicit_invocation: false
