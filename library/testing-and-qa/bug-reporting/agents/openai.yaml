version: 1
metadata:
  key: "bug-reporting"
interface:
  display_name: "Bug Reporting"
  short_description: "Use this skill when you need to write clear, reproducible bug reports with steps, environment details, and evidence; triggers include bug..."
  default_prompt: "Use the bug-reporting skill to complete this testing task with structured outputs and practical next steps."
policy:
  allow_implicit_invocation: true
