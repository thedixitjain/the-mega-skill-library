version: 1
metadata:
  key: "security-testing"
interface:
  display_name: "安全测试（中文版）"
  short_description: "Use this skill when you need to design security testing around OWASP risks, vulnerability scanning, and penetration scenarios; triggers i..."
  default_prompt: "Use the security-testing skill to complete this testing task with structured outputs and practical next steps."
policy:
  allow_implicit_invocation: true
