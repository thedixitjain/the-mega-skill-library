version: 1
metadata:
  key: "api-testing"
interface:
  display_name: "API 测试（中文版）"
  short_description: "Use this skill when you need to design API test plans or cases for REST, GraphQL, or gRPC interfaces; triggers include API 测试 and api tes..."
  default_prompt: "Use the api-testing skill to complete this testing task with structured outputs and practical next steps."
policy:
  allow_implicit_invocation: true
