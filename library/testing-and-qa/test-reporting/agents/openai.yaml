version: 1
metadata:
  key: "test-reporting"
interface:
  display_name: "Test Reporting"
  short_description: "Use this skill when you need to generate test reports with summary, metrics, defect analysis, and risk assessment; triggers include test..."
  default_prompt: "Use the test-reporting skill to complete this testing task with structured outputs and practical next steps."
policy:
  allow_implicit_invocation: true
