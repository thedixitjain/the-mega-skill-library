version: 1
metadata:
  key: "api-testing"
interface:
  display_name: "API Testing (English)"
  short_description: "Use this skill when you need to design API test plans or cases for REST, GraphQL, or gRPC interfaces; triggers include API testing and AP..."
  default_prompt: "Use the api-testing skill to complete this testing task with structured outputs and practical next steps."
policy:
  allow_implicit_invocation: true
