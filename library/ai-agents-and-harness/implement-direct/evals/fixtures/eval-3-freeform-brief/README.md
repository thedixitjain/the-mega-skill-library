# Freeform Brief Fixture

This fixture has no requirements.md, solution.md, or other context — the eval prompt itself is the brief. The orchestrator should treat the prompt text as the primary context document and proceed without invoking specify-meta.
