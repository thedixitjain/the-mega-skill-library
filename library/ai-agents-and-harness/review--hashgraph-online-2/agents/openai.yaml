interface:
  display_name: "Claude Code Review"
  short_description: "Args: --wait, --background, --base <ref>, --scope <auto|working-tree|branch>, --model <model>. Standard review of local git changes."
