---
name: loki-mode-claude
description: "Cross-cutting conventions for the Acme logical app. Members: ui (web front-end), api (REST surface), service (background worker). All members share the acme appid and the shared memory directory. Use Pydantic models for API contracts; the UI consumes via fetch()."
category: testing-and-qa
source_repo: asklokesh/loki-mode
source_path: "tests/fixtures/project-graph/acme/CLAUDE.md"
source_url: https://github.com/asklokesh/loki-mode/blob/HEAD/tests/fixtures/project-graph/acme/CLAUDE.md
---
# Acme App (Parent Context)

Cross-cutting conventions for the Acme logical app. Members:
ui (web front-end), api (REST surface), service (background worker).
All members share the `acme` app_id and the shared memory directory.
Use Pydantic models for API contracts; the UI consumes via fetch().

---

**Source:** [`asklokesh/loki-mode`](https://github.com/asklokesh/loki-mode) → `tests/fixtures/project-graph/acme/CLAUDE.md`
